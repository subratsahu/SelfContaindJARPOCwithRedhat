package lvgi.lvgirules.rating.ratingrules.IDVLimitingCheck;

import java.lang.Number;
import lvgi.lvgirules.model.MotorPremiumRatesResponse;

rule "MinimumIDVCheckFirstYear"
	dialect "mvel"
	no-loop true
	lock-on-active true
	salience 50
	agenda-group "IDVGuidedRules"
	when
		mpr : MotorPremiumRatesResponse( idv > 0.0 , idv < mpr.getMinIdv() )
	then
		modify( mpr ) {
				setIdv( mpr.getMinIdv() )
		}
end
