package lvgi.lvgirules.rating.ratingrules.IDVLimitingCheck;

import java.lang.Number;
import lvgi.lvgirules.model.MotorPremiumRatesResponse;

rule "MinimumIDVCheckFifthYear"
	dialect "mvel"
	no-loop true
	lock-on-active true
	salience 50
	agenda-group "IDVGuidedRules"
	when
		mpr : MotorPremiumRatesResponse( idv5 > 0.0 , idv5 < mpr.getMinIdv() )
	then
		modify( mpr ) {
				setIdv5( mpr.getMinIdv() )
		}
end
